# yaml-language-server: $schema=https://raw.githubusercontent.com/Ensono/taskctl/fix/config-schema-generator/internal/config/schemas/schema_v1.json

import:
  - tasks.yaml
  - tasks.yaml

tasks:
  task1:
    command:
      - echo true
