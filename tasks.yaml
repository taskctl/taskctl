pipelines:
  fixcs:
    - task: fmt
      dir: "{{.Root}}"

  prepare:
    - task: tidy
    - task: test
    - task: fmt
      dir: "{{.Root}}"
      depends_on: test
    - pipeline: lints
    - task: update-completers
      depends_on: test

  lints:
    - task: golangci-lint

tasks:
  test:
    description: Run all Go tests
    command:
      - go test ./...

  build-host:
    description: Build taskctl for the host platform into bin/taskctl (dogfooding binary)
    dir: "{{.Root}}"
    command:
      - go build -o bin/taskctl .

  build:
    description: Cross-compile bin/taskctl_{linux,darwin,windows} release binaries
    command:
      - GOOS=${GOOS} GOARCH=amd64 go build -o bin/taskctl_${GOOS} .
    variations:
      - GOOS: linux
      - GOOS: darwin
      - GOOS: windows

  golangci-lint:
    description: Lint with golangci-lint (config in .golangci.yaml)
    command:
      - golangci-lint run

  fmt:
    description: Format Go sources (gofmt -s + goimports) via golangci-lint
    dir: "{{.Root}}"
    command:
      - golangci-lint fmt

  goreleaser:
    description: Build a release snapshot with GoReleaser
    command:
      - goreleaser --clean

  tidy:
    description: Sync go.mod/go.sum with the source (go mod tidy)
    command:
      - go mod tidy

  update-completers:
    description: Regenerate bash/zsh completion scripts in autocomplete/
    dir: "{{.Root}}"
    command:
      - rm -rf autocomplete/*
      - go run . completion bash > autocomplete/bash_completion.bash
      - go run . completion zsh > autocomplete/zsh_completion.zsh
